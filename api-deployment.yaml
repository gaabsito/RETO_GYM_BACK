apiVersion: v1
kind: ConfigMap
metadata:
  name: gymapi-config
data:
  appsettings.json: |
    {
      "Logging": {
        "LogLevel": {
          "Default": "Information",
          "Microsoft.AspNetCore": "Warning"
        }
      },
      "AllowedHosts": "*",
      "ConnectionStrings": {
        "GymappDB": "Server=database-1.cxscdcmrigs9.us-east-1.rds.amazonaws.com;Database=GymappDB;User Id=SA;Password=${DB_PASSWORD};TrustServerCertificate=True;"
      },
      "JwtSettings": {
        "SecretKey": "${JWT_SECRET}",
        "ExpirationMinutes": 1440
      },
      "EmailSettings": {
        "SmtpServer": "smtp.gmail.com",
        "SmtpPort": 587,
        "SmtpUsername": "entrenategymapp@gmail.com",
        "SmtpPassword": "${EMAIL_PASSWORD}",
        "FromEmail": "entrenategymapp@gmail.com",
        "FromName": "ENTRÉNATE"
      },
      "Authentication": {
        "Google": {
          "ClientId": "1088462991637-hqvnr9hmlqpv6lfin0sga0o1h5k4kv21.apps.googleusercontent.com",
          "ClientSecret": "${GOOGLE_CLIENT_SECRET}"
        }
      },
      "FrontendUrl": "http://frontend-service"
    }
---
apiVersion: v1
kind: Secret
metadata:
  name: gymapi-secrets
type: Opaque
stringData:
  db-password: YourStrongPassw0rd
  jwt-secret: zxT3u9n6f8p1m4k7LQJHGFDSAWERTY_secureKey_2024
  email-password: pzzb qmpb jbtr iqgf
  google-client-secret: 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
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gymapi-deployment
  labels:
    app: gymapi
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gymapi
  template:
    metadata:
      labels:
        app: gymapi
    spec:
      containers:
      - name: gymapi
        image: jaimeescartinsv/sanvalero:gymapi
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "1"
            memory: "512Mi"
          requests:
            cpu: "200m"
            memory: "256Mi"
        volumeMounts:
        - name: config-volume
          mountPath: /app/appsettings.json
          subPath: appsettings.json
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: ASPNETCORE_URLS
          value: "http://+:80"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: gymapi-secrets
              key: db-password
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: gymapi-secrets
              key: jwt-secret
        - name: EMAIL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: gymapi-secrets
              key: email-password
        - name: GOOGLE_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: gymapi-secrets
              key: google-client-secret
      volumes:
      - name: config-volume
        configMap:
          name: gymapi-config
---
apiVersion: v1
kind: Service
metadata:
  name: gymapi-service
spec:
  selector:
    app: gymapi
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gymapi-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rules:
  - host: api.gymapp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: gymapi-service
            port:
              number: 80